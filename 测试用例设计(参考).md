# CartFlow 测试用例设计

## 测试目标

验证购物车结算模块是否满足下面 13 条核心需求，重点覆盖输入校验、库存校验、金额计算、运费规则、状态流转、客户端金额篡改、性能和结果稳定性。

1. 用户必须能够对购物车中的一个或多个商品进行结算。每个商品应包含商品单价、购买数量和当前库存信息。
2. 系统应验证购物车和每个商品的输入是否合法。购物车不能为空；商品单价必须大于0，购买数量必须为整数且大于0，且购买数量不能超过当前库存。
3. 若购物车为空，或任一商品存在单价非法、购买数量非法、库存不足的情况，系统应拒绝结算，并显示对应错误信息，例如“购物车不能为空”“商品单价非法”“购买数量非法”“库存不足”。
4. 系统应根据每个商品的单价和购买数量计算商品小计，并将所有商品小计相加得到订单原价。商品小计 = 商品单价 × 购买数量，订单原价 = 所有商品小计之和。
5. 系统应根据订单原价计算运费。当订单原价大于或等于200元时，系统免收运费；当订单原价小于200元时，系统收取10元运费。
6. 系统应按照“输入校验、商品小计计算、订单原价计算、运费计算、最终金额计算”的顺序完成结算。若输入校验失败，系统应立即终止结算流程并返回失败状态，不继续进行金额计算。
7. 最终应付金额 = 订单原价 + 运费。最终应付金额不能小于0，并且金额结果应保留两位小数。
8. 当所有商品信息合法、库存充足且金额计算正常时，系统应允许结算成功，并返回订单原价、运费、最终应付金额和结算成功状态。
9. 当购物车为空、商品单价非法、购买数量非法、库存不足或金额计算异常时，系统应拒绝结算，返回结算失败状态，并保留购物车内容。
10. 购物车结算过程应包含“待结算、校验中、结算成功、结算失败”四种状态。用户提交结算请求后，订单状态由“待结算”变为“校验中”；校验全部通过后变为“结算成功”；任一校验失败后变为“结算失败”。
11. 系统不得直接信任客户端传入的最终金额，最终金额必须由系统根据商品单价、购买数量、库存和运费规则重新计算。
12. 单个购物车结算请求的处理时间应少于200毫秒。
13. 对于相同输入，系统应返回稳定且一致的计算结果。

## 测试范围

- 核心结算函数：`checkout_cart`
- API 接口：`POST /api/checkout`
- 不覆盖前端样式，仅通过前端状态相关规则设计必要验证点

## 测试用例矩阵

| 编号 | 覆盖需求   | 测试场景             | 输入数据                         | 预期结果                                                      |
| ---- | ---------- | -------------------- | -------------------------------- | ------------------------------------------------------------- |
| TC01 | 1, 8       | 单商品合法结算       | 单价 99.99，数量 1，库存 2       | 结算成功，返回小计 99.99、原价 99.99、运费 10.00、最终 109.99 |
| TC02 | 1, 4, 8    | 多商品合法结算       | 50×2，25.50×4                    | 两个小计分别为 100.00、102.00，订单原价 202.00                |
| TC03 | 2, 3, 9    | 购物车为空           | 空列表                           | 结算失败，错误信息“购物车不能为空”，金额为 0.00               |
| TC04 | 2, 3, 6, 9 | 商品单价为 0         | 单价 0，数量 1，库存 1           | 结算失败，错误信息“商品单价非法”，不计算小计                  |
| TC05 | 2, 3, 6, 9 | 商品单价为负数       | 单价 -1，数量 1，库存 1          | 结算失败，错误信息“商品单价非法”                              |
| TC06 | 2, 3, 6, 9 | 购买数量为 0         | 单价 10，数量 0，库存 1          | 结算失败，错误信息“购买数量非法”                              |
| TC07 | 2, 3, 6, 9 | 购买数量非整数       | 单价 10，数量 1.5，库存 3        | 结算失败，错误信息“购买数量非法”                              |
| TC08 | 2, 3, 6, 9 | 库存不足             | 单价 10，数量 3，库存 2          | 结算失败，错误信息“库存不足”                                  |
| TC09 | 5, 7       | 订单原价小于 200     | 原价 199.99                      | 运费 10.00，最终金额 209.99                                   |
| TC10 | 5, 7       | 订单原价等于 200     | 原价 200.00                      | 运费 0.00，最终金额 200.00                                    |
| TC11 | 5, 7       | 订单原价大于 200     | 原价 202.00                      | 运费 0.00，最终金额 202.00                                    |
| TC12 | 7          | 金额保留两位小数     | 单价 19.999，数量 1              | 金额按两位小数返回                                            |
| TC13 | 10         | 状态流转             | 前端提交一次合法/非法请求        | 提交后先显示“校验中”，后端结果为“结算成功”或“结算失败”        |
| TC14 | 11         | 客户端篡改最终金额   | 实际应付 110.00，但客户端传 0.01 | 正确系统应返回 110.00；当前故意缺陷会返回 0.01，测试应失败    |
| TC15 | 12         | 性能                 | 100 个合法商品                   | 处理时间小于 200ms                                            |
| TC16 | 13         | 稳定性               | 同一输入执行两次                 | 两次结果完全一致                                              |
| TC17 | 9          | 失败时保留购物车内容 | 库存不足商品                     | 响应 items 保留原始单价、数量、库存，subtotal 为 null         |

## 测试数据设计原则

- 等价类：合法商品、空购物车、非法单价、非法数量、库存不足。
- 边界值：单价 0、数量 0、数量等于库存、数量大于库存、订单原价 199.99、200.00、200.01。
- 异常路径：任一商品非法时应立即失败，后续金额字段保持 0.00。
- 安全性：客户端传入 `final_amount` 时，服务端必须重新计算，不能直接采用客户端金额。

## 预期演示效果

当前代码已被故意改成“不满足需求 11”：当请求中包含 `final_amount` 时，后端会错误地信任客户端金额。

因此运行 pytest 时，需求 11 对应测试应失败，用来证明测试用例能够发现实现缺陷。
